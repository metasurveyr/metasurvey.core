% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metasurvey.core-package.R
\name{metasurvey.core-options}
\alias{metasurvey.core-options}
\title{Options used by metasurvey.core}
\value{
No return value. This topic documents the global options
recognized by metasurvey.core and their defaults.
}
\description{
All behavior toggles of metasurvey.core are controlled through R
\code{\link[=options]{options()}} under the \code{metasurvey.} prefix. None of them needs to be
set explicitly: every option has a working default.
}
\section{Options}{

\describe{
\item{\code{metasurvey.engine}}{Character. Data-processing engine used
when loading surveys. Default \code{"data.table"} (set on load). Change
it with \code{\link[=set_engine]{set_engine()}}; see \code{\link[=show_engines]{show_engines()}} for the available
engines.}
\item{\code{metasurvey.lazy_processing}}{Logical, default \code{TRUE}. When
\code{TRUE}, \verb{step_*()} functions record transformations without
executing them until \code{\link[=bake_steps]{bake_steps()}} is called. Set with
\code{\link[=set_lazy_processing]{set_lazy_processing()}}; query with \code{\link[=lazy_default]{lazy_default()}}.}
\item{\code{metasurvey.use_copy}}{Logical, default \code{TRUE}. When \code{TRUE},
steps operate on a copy of the survey so the original object is
never modified in place. Set with \code{\link[=set_use_copy]{set_use_copy()}}; query with
\code{\link[=use_copy_default]{use_copy_default()}}.}
\item{\code{metasurvey.verbose}}{Logical, default \code{TRUE}. Gates all
informational messages emitted by the package. Set it to \code{FALSE}
to silence them; errors and warnings are not affected.}
\item{\code{metasurvey.backend}}{A \code{RecipeBackend} object holding the
active recipe backend. Unset by default (an in-memory local
backend is used). Configure with \code{\link[=set_backend]{set_backend()}}; query with
\code{\link[=get_backend]{get_backend()}}.}
\item{\code{metasurvey.workflow_backend}}{A \code{WorkflowBackend} object
holding the active workflow backend. Unset by default (an
in-memory local backend is used). Configure with
\code{\link[=set_workflow_backend]{set_workflow_backend()}}; query with \code{\link[=get_workflow_backend]{get_workflow_backend()}}.}
\item{\code{metasurvey.backend_provider}}{A function \verb{(op, args)} that
implements the remote (\code{"api"}) backend. Default \code{NULL}: core has
no network access, and selecting the \code{"api"} backend without a
registered provider signals a
\code{metasurvey_error_backend_unavailable} error (see
\link{metasurvey_conditions}). Companion packages such as
\code{metasurvey.explorer.backend} register the provider on load.}
\item{\code{metasurvey.skip_recipes}}{Logical, default \code{FALSE}. When
\code{TRUE}, \code{\link[=get_recipe]{get_recipe()}} skips every backend lookup and returns
\code{NULL} with a warning: useful for fully offline runs.}
}
}

\examples{
# Silence informational messages for a session
old <- options(metasurvey.verbose = FALSE)
options(old)

# Current engine and laziness
getOption("metasurvey.engine")
lazy_default()
}
