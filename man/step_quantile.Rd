% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/steps.R
\name{step_quantile}
\alias{step_quantile}
\title{Weighted quantile groups (step)}
\usage{
step_quantile(
  svy,
  new_var,
  x,
  n = 5,
  weight = NULL,
  .by = NULL,
  .copy = use_copy_default(),
  comment = "Quantile step",
  lazy = lazy_default(),
  record = TRUE
)
}
\arguments{
\item{svy}{A \link{Survey} or \link{RotativePanelSurvey} object.}

\item{new_var}{Name of the new group variable (unquoted or character).}

\item{x}{Name of the numeric variable to split into quantile groups
(unquoted or character). Must be an existing column.}

\item{n}{Number of groups (default \code{5}, i.e. quintiles). Use \code{10} for
deciles, \code{4} for quartiles, etc.}

\item{weight}{Name of the weight column (unquoted or character).
Defaults to the survey's own weight.}

\item{.by}{Optional character vector of grouping variables: quantile
breaks are computed independently within each group.}

\item{.copy}{Whether to operate on a copy (default: \code{\link[=use_copy_default]{use_copy_default()}}).}

\item{comment}{Descriptive text for the step (default \code{"Quantile step"}).}

\item{lazy}{Internal. Currently ignored: the groups are always computed
immediately and the step is recorded as executed.}

\item{record}{Internal. Whether to record the step (default \code{TRUE}).}
}
\value{
The survey object with \code{new_var} added (integer codes \verb{1..n})
and the step recorded.
}
\description{
Creates a step that assigns each row to a weighted quantile group
(quintiles, deciles, ...) of a numeric variable, e.g. income quintiles
at the household level. Quantiles are computed directly on the weighted
ECDF, without expanding the data by the weights.
}
\details{
\strong{Execution model:} The quantile groups are computed immediately when
the step is created and the step is recorded as already executed
(\code{bake = TRUE}). Calling \code{\link[=bake_steps]{bake_steps()}} afterwards is safe: executed
steps are skipped.

\strong{Algorithm:} The break for probability \code{p} is the right-continuous
inverse of the weighted ECDF, \verb{inf\{x : F_w(x) >= p\}}. This matches
\code{survey::svyquantile(qrule = "math")} and, for integer weights,
\code{quantile(rep(x, w), type = 1)}, but runs in \verb{O(n log n)} instead of
\code{O(sum(w))} memory. Rows are then assigned with intervals
\verb{(q[k-1], q[k]]}; rows with \code{NA} in \code{x} get \code{NA}. If breaks are tied
(heavily discrete \code{x}), fewer than \code{n} groups can result.

For household-level quantiles (one observation per household), collapse
first with \code{\link[=step_collapse]{step_collapse()}} or compute on a household-level survey.
}
\examples{
dt <- data.table::data.table(
  id = 1:10, income = c(1:10) * 100, w = rep(1, 10)
)
svy <- Survey$new(
  data = dt, edition = "2023", type = "test",
  psu = NULL, engine = "data.table", weight = add_weight(annual = "w")
)
svy <- step_quantile(svy, income_q, income, n = 5)
get_data(svy)$income_q

}
\seealso{
\code{\link[=step_collapse]{step_collapse()}} to collapse to household level before
computing household quantiles.

Other steps:
\code{\link[=bake_steps]{bake_steps()}},
\code{\link[=get_steps]{get_steps()}},
\code{\link[=step_collapse]{step_collapse()}},
\code{\link[=step_compute]{step_compute()}},
\code{\link[=step_filter]{step_filter()}},
\code{\link[=step_join]{step_join()}},
\code{\link[=step_recode]{step_recode()}},
\code{\link[=step_remove]{step_remove()}},
\code{\link[=step_rename]{step_rename()}},
\code{\link[=step_validate]{step_validate()}},
\code{\link[=view_graph]{view_graph()}}
}
\concept{steps}
\keyword{step}
